<style>
    body {
        background-image: url("/background3.jpg");
        margin: 0;
        width: 100%;
        height: 100%;
        backgroud-repeat: no-repeat;
        background-size: cover;
    }
</style>

@{
    ViewBag.Title = "Home Page";
}


<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">


<div class="row boxes">
    <div class="col-md-12">
        @if (User.IsInRole("Admin"))
        {
            <div class="col-md-4 front-box">
                <p>Administrare utilizatori</p>
                <a href="/Users"><i class="glyphicon glyphicon-user"></i></a>

            </div>
        }
        @if (User.IsInRole("Membru") || User.IsInRole("Organizator") || User.IsInRole("Admin"))
        {
            <div class="col-md-4 front-box" style="background-color:grey">
                <p>Materii</p>
                <a href="/Subject"><i class="glyphicon glyphicon-list-alt"></i></a>
            </div>

			<div class="col-md-4 front-box" style="background-color:grey">
				<p>Adăugare materie</p>
				<a href="/Subject/New"><i class="glyphicon glyphicon-plus"></i></a>
			</div>
        }
    </div>

    @if (!User.IsInRole("Membru") && !User.IsInRole("Organizator") && !User.IsInRole("Admin"))
    {
        <a class="col-md-12 front-box2" href = "/Account/Register">
            <h2>
               
                                                                          
            </h2>
            <br />
            <h2 class="text-center"> Join now!</h2>
        </a>
    }


</div>


